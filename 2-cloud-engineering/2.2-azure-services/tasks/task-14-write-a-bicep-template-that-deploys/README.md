# Task 14: Write a Bicep Template That Deploys a Full Web App Stack

## Objective

@description('Environment name (dev, staging, prod)') @allowed(['dev', 'staging', 'prod']) param environment string

<!-- LOG_OUTPUT -->
