# Task 2: 

## Objective

Create an IAM policy that allows EC2 instance management, but only in `us-east-1` and `eu-west-1`, and only for `t3.micro`, `t3.small`, and `t3.medium` instance types.

<!-- LOG_OUTPUT -->
