# Task 1: Deploy to AWS + GCP Using Terraform — Test Failover

## Objective

Deploy the same application (a simple web server) to both AWS and GCP using Terraform. Demonstrate that if you point your DNS to the GCP deployment, the application continues to work — simulating failover.

<!-- LOG_OUTPUT -->
