# Task 2: Set Up Airflow on Kubernetes — Build a DAG That Runs Daily ETL

## Objective

Deploy Airflow on a Kubernetes cluster using the KubernetesExecutor, then build a DAG that runs a daily ETL pipeline.

<!-- LOG_OUTPUT -->
